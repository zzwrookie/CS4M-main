# Writing Rationale Matrix

| Row ID | Manuscript Unit | Current Problem or Planned Function | Motivation Link | Reference/SOTA Pattern Learned | Target Scene or Venue Norm | Evidence or Citation Anchor | Planned Change or Text Move | Final Text Check |
|---|---|---|---|---|---|---|---|---|
| F1 | Whole-paper framework and throughline | The current draft has strong method fragments but no complete paper architecture. The planned function is to make the whole manuscript follow one framework: operational gap, stricter online model, CS4M design response, evidence, and limitations. | The motivation is the spine: CS4M changes the online detection unit from a completed provenance graph/window/segment to the individual arriving event. Every section must either establish that gap, explain the design required by it, or test it with evidence. | The SOTA pattern learned from PIDS papers is to organize around a system property such as attribution quality, practicality, or online detection. The reference pattern to transfer is not wording, but the disciplined sequence from field problem to design property to evaluation. | A USENIX Security conference paper needs a precise deployment claim, threat model, implementation story, reproducible evaluation, and fair related work. The venue norm is evidence-constrained systems writing, not broad model marketing. | Evidence anchors are `docs/CS4M_UsenixSecurity.tex`, `source_map.md`, `evidence_bank.md`, online alert artifacts, reproduction manifests, and citation candidates C01-C10. | Reframe the full text so the Introduction promises event-arrival online alerting, Design explains the compact semantic state that makes it possible, Evaluation tests that promise, and Discussion bounds it. This is a structural rewrite, not a polish pass. | Pass if a reviewer can state the problem, gap, design response, evidence, and limitation from section openings and figure/table callouts without seeing unsupported claims. |
| T1 | Title | The current title says "real-time semantic-state" but does not explicitly distinguish event-arrival alerting from pseudo-real-time graph processing. | The motivation requires the title to signal the stricter online unit, because this is the paper's main contribution rather than Word2Vec or SSM alone. | Reference papers often encode the central system property in the title: practical investigation, attribution quality, runtime detection, or online APT detection. | The target venue favors concise, falsifiable titles that do not overclaim. | Confirmed motivation, draft title, and SOTA title patterns from C01-C08. | Rewrite the title toward "CS4M: Event-Arrival Online Provenance Intrusion Detection with Compact Semantic State" or a shorter equivalent. The text move is to foreground the deployment model before the mechanism. | Pass if the title names event-arrival or true online provenance detection and avoids unsupported claims such as universal superiority. |
| A1 | Abstract opening | The draft has no complete abstract, so the first reader touchpoint does not establish the gap. | The motivation link is direct: the abstract must say that online log collection is not the same as online alert semantics. | The reference/SOTA pattern is to open with a concrete operational problem before naming the system. "Sometimes Simpler is Better" supports a deployment-centered opening rather than architecture-first framing. | USENIX abstracts need a compact statement of problem, gap, approach, and evidence. | Citation anchors C01-C05 and source anchors EB1-EB3. | Write an opening sentence that contrasts graph/window/segment pipelines with event-arrival alerting. The planned text move is a fair contrast: prior PIDSs are valuable, but their alerting unit often delays immediate deployment. | Pass if the opening is understandable without method jargon and does not accuse prior systems with unqualified "not real-time" language. |
| A2 | Abstract contribution and evidence summary | The manuscript needs a short evidence-backed abstract contribution sentence, not a list of mechanisms. | The motivation requires the abstract to present CS4M as a design response to the alerting-unit gap. | SOTA abstracts typically pair a named system with one differentiating property and a concise evaluation claim. | Venue norm: numerical claims must be backed by artifacts and should not be invented. | Evidence anchors EB1-EB8; exact metrics must be extracted before final. | Draft the abstract contribution as "CS4M scores each event before state update using compact semantic state and emits event/node evidence immediately." Leave metric placeholders until verified. | Pass if every number in the abstract has a local source or is omitted. |
| I1 | Introduction field problem paragraph | The draft starts at Section 3 and lacks a reader-friendly field problem. | The motivation needs a field-level reason why alert timing matters for PIDS deployment. | KAIROS, HOLMES, UNICORN, and ORTHRUS show provenance is useful for investigation, correlation, runtime detection, and attribution. | A conference Introduction should establish importance before narrowing to the gap. | Citation anchors C02-C08 and draft problem text. | Add an opening paragraph that values provenance graphs for APT context while setting up immediate alerting as a stricter operational problem. The text move is balance before critique. | Pass if the paragraph makes graphs useful but insufficient for immediate event alerting. |
| I2 | Introduction pseudo-real-time gap paragraph | The current pseudo-real-time critique is promising but needs tighter and fairer wording. | The motivation is to define pseudo-real-time as a mismatch between log stream availability and alert generation semantics. | The SOTA pattern is a measured limitation statement: graph construction, segmentation, and periodic inference can be valid design choices, but they choose a different inference unit. | Venue norm: related-work critique must be technically specific and citation-supported. | C01, C03, C04, C05, C08; draft Sec. 3.1. | Rewrite the gap paragraph around "alerting unit" rather than saying systems are simply not real-time. The planned move is to compare event, window, segment, and graph as computational objects. | Pass if the paragraph can withstand review by authors of graph PIDS papers. |
| I3 | Introduction CS4M response paragraph | The draft has CS4M details later but no front-loaded response. | The motivation requires naming CS4M as the system that scores the arriving event before it updates state. | SOTA systems papers introduce the system as a direct answer to the stated bottleneck. | Venue norm: the response paragraph should be concrete but not overly detailed. | EB10, S9 source paths, Algorithm 1 draft. | Add a paragraph that states CS4M maintains compact semantic state, reads pre-update states, predicts event semantics, scores residuals, emits evidence, and only then updates memory. | Pass if the paragraph clearly states score-before-update and no graph materialization. |
| I4 | Contributions paragraph | The draft lacks a contribution list, making mechanisms compete with each other. | The motivation should control contribution ordering: event-arrival model first, mechanisms second, evidence third. | Exemplar papers typically list a small number of contributions tied to evaluation. | USENIX norm: contributions must be testable and not inflated. | Evidence anchors EB1-EB9 and citation bank. | Write four contributions: stricter online model, compact semantic-state design, explainable online alerts, and cross-OS/evidence-backed evaluation/audit. | Pass if each contribution maps to a section and evidence source. |
| B1 | Background and motivation section | Related systems and provenance basics are currently embedded in problem text, so source evidence and citation anchors are hard to audit. | The motivation benefits from separating background value from the specific gap. | ORTHRUS and KAIROS show that attribution/investigation are important but distinct from alert timing. | Venue norm: readers need fair background before formal problem statement. | C02-C08 and source evidence from the existing draft. | Create a section that explains provenance entities, graph-first PIDS, and detection/attribution/investigation distinctions. The text move is to prevent false comparison. | Pass if CS4M is positioned as complementary to reconstruction systems, not a replacement for all provenance analysis. |
| P1 | Formal online detection model | The existing event tuple and alert definition are good but isolated. | The motivation requires a formal definition of event-arrival online detection. | SOTA papers often define the task before method details so reviewers can test whether design matches task. | USENIX security norm: threat model and assumptions must be explicit. | Draft Sec. 3.3, AGENTS leakage rules, tests. | Move the event stream definition into a formal section with score-before-update, no future event use, no test-label leakage, and post-stream evaluation. | Pass if the model forbids windows/future statistics for alert generation. |
| P2 | Threat model and leakage boundary | Current text states attacker assumptions but should better separate runtime and evaluation, and the evidence source for leakage controls should be visible. | The motivation would fail if CS4M used future labels or test rankings to alert. | PIDS evaluation papers emphasize leakage and clean separation between detection and evaluation. | Venue norm: security papers scrutinize threat model and evaluation hygiene. | AGENTS rules, tests, scripts, evaluation artifacts, and citation support for evaluation hygiene. | Add a concise threat model and no-leakage paragraph: runtime uses only current/past runtime-visible fields; GT is only post-stream. | Pass if threshold and policy design are clearly validation-only or runtime-visible. |
| D1 | CS4M design overview | The current design is detailed but lacks a requirement-to-mechanism map. | The motivation requires showing why each component exists for event-arrival online alerting. | Systems papers often include a table mapping challenges to design choices. | Venue norm: architecture before equation depth. | Draft Challenges/Solutions section and Algorithm 1. | Add a design-principles table: no full graph -> semantic state; memory growth -> OFSM; anomaly self-explanation -> score-before-update; evidence -> alert outputs. | Pass if the reader can understand the architecture before equations. |
| D2 | Semantic tokenization and embeddings | The draft describes Word2Vec, but the paper could overemphasize it as the novelty. | The motivation says semantics are an enabler of per-event scoring and cross-OS portability, not the main contribution. | SOTA graph-learning papers use graph embeddings; CS4M uses runtime-visible semantic tokens with audits. | Venue norm: explain features without label-derived rules. | Semantic audit EB5, CADETS audit EB6, projection EB7. | Reframe Word2Vec as lightweight semantic representation over runtime-visible tokens. Add caveat that semantic audits are post-hoc and rules are label-free. | Pass if no sentence implies GT created tokenizer rules. |
| D3 | Causal semantic state and E4 dual residual prediction | Existing equations explain state and low-rank prediction but need a clearer motivation hook and must identify E4 dual as the paper method. | The motivation depends on pre-update state being the detector's compact causal memory. E4 dual makes the online scoring target explicit for stateful and both-cold events. | SSM references C09-C10 support efficient state modeling, while PIDS SOTA motivates avoiding full graph inference. | Venue norm: math should serve design, not overwhelm. | Draft Sec. 4.2 and 4.4, EB10, E4 runner and conditional-head implementation. | Keep state equations, then define event_semantic_target and both_cold_action_target, with target-case-specific low-rank heads. | Pass if E4 dual is the method spine and no E2/E5 variant is presented as a main contribution. |
| D4 | OFSM memory control | OFSM currently has useful proof material but needs deployment framing and an explicit motivation link to event-arrival online memory pressure. | Bounded state is essential because event-arrival online detection cannot keep an unbounded historical graph in memory. | "Sometimes Simpler is Better" supports the deployability/simplicity pattern; UNICORN supports runtime graph growth concerns. | Venue norm: asymptotic bounds should be tied to implementation evidence. | Draft OFSM, reproduction manifest EB8, memory artifacts. | Rewrite and move OFSM as logical-node to physical-slot mapping with copy-on-write; keep proofs in appendix and use main text for intuition and bounds. | Pass if memory claims distinguish theoretical bound from measured RSS. |
| D5 | Exclude E2/E5 variants from the main method | Earlier planning treated E5 as a possible method mechanism, but the confirmed paper spine is E4 dual. | The motivation is stronger when the method is a single deployable online path rather than a bundle of exploratory variants. | Systems papers should keep the main design path explicit and move variants to ablation only when verified. | Venue norm: avoid presenting unsupported branches as contributions. | User clarification that E4 dual is the main line and E2/E5 should not be written into the paper method. | Remove E5 update-gate equations and E2/E5 method language from Sections 3-7; retain only dataset names such as ClearScope E5 where they are evaluation rows. | Pass if the method sections describe E4 dual, score-before-update, OFSM, thresholds, and alert evidence without branch confusion. |
| D6 | Threshold calibration and suppression | Existing scoring text should emphasize validation-only calibration and the reference pattern of separating training, validation, runtime alerting, and post-stream evaluation. | The motivation's deployment model requires runtime thresholds that do not use test labels or rankings. | PIDS evaluation critiques often focus on practical validity and leakage risks. | Venue norm: security evaluations must be reproducible and leakage-free. | Tests, specs, metrics configs. | Rewrite the scoring text to clarify group thresholds, conservative fallback, pair-only suppression, and why endpoint-only suppression can hide TP evidence. | Pass if all policy inputs are runtime-visible or validation-only. |
| R1 | Real-time alerting algorithm | Algorithm 1 is the clearest artifact for proving event order. | The motivation depends on exact order: read old state, score, alert, then update. | Systems papers often use pseudocode to establish causality and implementation feasibility. | Venue norm: algorithm must match implementation and not include disabled branches. | Draft Algorithm 1, source paths S9. | Polish Algorithm 1 as a central figure and align each step with implementation files. | Pass if the algorithm cannot be misread as batch/window inference. |
| E1 | Evaluation blueprint | The draft lacks a complete evaluation section and should not invent results. | The motivation determines evaluation questions: true online behavior, quality, resource cost, cross-OS behavior, explainability. | SOTA systems papers state evaluation questions before tables. | Venue norm: metrics and claims must be evidence-backed. | EB1-EB9. | Create evaluation section placeholders tied to exact artifacts. Extract numbers in the next phase, not during this planning stage. | Pass if each planned table has a source path and caveat. |
| E2 | Cross-OS semantic analysis | Semantic audit work is valuable but currently outside the manuscript, and it needs an explicit motivation, reference, evidence, and text-move anchor. | Cross-OS semantic discipline supports the claim that CS4M is deployable beyond one OS. | Exemplar systems and SOTA papers use case studies to show generality; CS4M should use audits to show both strengths and limitations. | Venue norm: diagnostic results must be labeled as diagnostic, not runtime detection. | EB5, EB6, EB7. | Move audit evidence into a case-study section for FreeBSD, Linux, Android, and Windows semantics, with GT used only post-hoc. | Pass if CADETS v3 projection is described as candidate projection, not final performance. |
| RW1 | Related work taxonomy | Current related-work content is mixed into limitations and motivation. | The motivation needs related work organized by alerting unit and objective. | SOTA pattern: compare by task and system assumption, not by chronological list. | Venue norm: fair, complete, and non-dismissive related work. | C01-C30 after verification. | Organize related work into graph-first PIDS, segmentation/window online PIDS, attribution/triage, runtime provenance, and state/sequence models. | Pass if TAPAS/ORTHRUS/KAIROS comparisons are precise and verified. |
| L1 | Discussion and limitations | The draft has theoretical appendix but limited discussion of boundaries. | The motivation is strongest when it admits what CS4M does not solve: full attribution, all OS semantics, and unverified candidate policies. | USENIX papers preserve trust through explicit limitations. | Venue norm: limitations should be concrete and not generic. | Confirmed motivation, EB6, EB7, citation bank. | Add a limitations section that treats graph reconstruction as complementary, semantic rules as OS-specific, and GT audits as post-stream only. | Pass if limitations narrow claims without undermining the event-arrival contribution. |
